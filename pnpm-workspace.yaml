packages:
  - "apps/*"
  - "packages/*"

# Allow these dependencies to run install/build scripts (pnpm 11 blocks by default).
onlyBuiltDependencies:
  - "@prisma/client"
  - "@prisma/engines"
  - prisma
  - esbuild
